@model MusicHouse

@{
    ViewData["Title"] = "Delete for the Music House";
}

<div class="container">
    <div class="row text-white">
        <div class="col-md-8 offset-md-2" style="margin-top: 30px; margin-bottom: 10px;">
            <h4 class="text-purple">Delete @Model.Name</h4>
        </div>
    </div>

    <div class="row">
        <div class="col-md-8 offset-md-2">
            <div class="form-group text-center mb-4">
                <img class="border-purple rounded-circle" style="max-width: 150px" id="ProfilePicturePreview" />
            </div>
            <form asp-action="Delete" method="post">
                <input type="hidden" asp-for="Id" />

                <div class="form-group mb-3">
                    <label asp-for="ProfilePictureUrl" class="control-label text-purple" style="font-size: 1.1rem;"></label>
                    <input asp-for="ProfilePictureUrl" class="form-control bg-lighter-dark text-white border-purple" readonly />
                    <span asp-validation-for="ProfilePictureUrl" class="text-danger"></span>
                </div>

                <div class="form-group mb-3">
                    <label asp-for="Name" class="control-label text-purple" style="font-size: 1.1rem;"></label>
                    <input asp-for="Name" class="form-control bg-lighter-dark text-white border-purple" readonly />
                    <span asp-validation-for="Name" class="text-danger"></span>
                </div>

                <div class="form-group mb-3">
                    <label asp-for="Bio" class="control-label text-purple" style="font-size: 1.1rem;"></label>
                    <input asp-for="Bio" class="form-control bg-lighter-dark text-white border-purple" readonly />
                    <span asp-validation-for="Bio" class="text-danger"></span>
                </div>

                <div class="form-group mb-3">
                    <button type="submit" class="btn btn-danger btn-sm float-right d-flex align-items-center justify-content-center px-4 py-2">
                        <i class="bi bi-trash me-1"></i> Confirm
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        $(document).ready(function () {
            var output = document.getElementById('ProfilePicturePreview');
            output.src = $("#ProfilePictureUrl").val();
        });
    </script>
}

<style>
    body {
        background-color: #F3F4F8;
    }

    .text-purple {
        color: #6C5B7B; /* Lighter purple */
        margin-bottom: 3px;
    }

    .form-control.bg-lighter-dark {
        background-color: #444;
        border-color: #6C5B7B; /* Lighter purple */
        color: #FFF;
    }

    .btn-danger {
        background-color: #e74c3c;
        border-color: #e74c3c;
        color: white;
        border-radius: 10px;
    }

        .btn-danger:hover {
            background-color: #c0392b;
        }

    .border-purple {
        border-color: #6C5B7B; /* Lighter purple */
    }
</style>
